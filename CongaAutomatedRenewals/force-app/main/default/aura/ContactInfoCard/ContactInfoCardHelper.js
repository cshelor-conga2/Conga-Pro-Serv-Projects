({
    helperMethod : function(component) {

    },

    bindContactInfo : function (component,data){
        

        if (data.userSessionData && typeof data.contactId != undefined){
            var contactId = data.contactId;
        }
    }
})